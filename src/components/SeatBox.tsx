import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const SeatBox = ({style}:any) => {
  return (
    <View style={style}>
      <Text>SeatBox</Text>
    </View>
  )
}

export default SeatBox

const styles = StyleSheet.create({})