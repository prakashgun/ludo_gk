import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Path = ({style}:any) => {
  return (
    <View style={style}>
      <Text>Path</Text>
    </View>
  )
}

export default Path

const styles = StyleSheet.create({})